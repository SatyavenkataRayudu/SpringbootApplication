apiVersion: v1
kind: Service
metadata:
  name: springboot-app-service
  namespace: production
  labels:
    app: springboot-app
spec:
  type: ClusterIP
  selector:
    app: springboot-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: ClientIP
